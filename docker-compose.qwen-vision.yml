services:
  qwen-vision:
    image: ${QWEN_VISION_IMAGE:-ghcr.io/imanmostafavi/dspark-qwen-vision@sha256:0898f08028ffc48d5f232d750c58ea8cac9e434ec21d3b875c6c62a945acb2a3}
    pull_policy: if_not_present
    entrypoint: []
    network_mode: host
    ipc: host
    shm_size: "64gb"

    ulimits:
      memlock: -1
      stack: 67108864

    gpus: all

    devices:
      - /dev/infiniband:/dev/infiniband

    volumes:
      - ${HF_CACHE:-${HOME}/.cache/huggingface}:/cache/huggingface
      - ${QWEN_TMP_HOST:-${HOME}/.cache/qwen-vision}:/tmp

    environment:
      HF_HOME: /cache/huggingface
      HF_HUB_OFFLINE: "${HF_HUB_OFFLINE:-0}"
      TRANSFORMERS_OFFLINE: "${TRANSFORMERS_OFFLINE:-0}"
      NCCL_NET: "${NCCL_NET:-IB}"
      NCCL_IB_DISABLE: "${NCCL_IB_DISABLE:-0}"
      NCCL_IB_HCA: "${NCCL_IB_HCA}"
      NCCL_SOCKET_IFNAME: "${NCCL_SOCKET_IFNAME}"
      TP_SOCKET_IFNAME: "${TP_SOCKET_IFNAME:-${NCCL_SOCKET_IFNAME}}"
      GLOO_SOCKET_IFNAME: "${GLOO_SOCKET_IFNAME:-${NCCL_SOCKET_IFNAME}}"
      NCCL_IB_GID_INDEX: "${NCCL_IB_GID_INDEX:-}"
      NCCL_IB_ADDR_FAMILY: "${NCCL_IB_ADDR_FAMILY:-AF_INET}"
      NCCL_IB_ROCE_VERSION_NUM: "${NCCL_IB_ROCE_VERSION_NUM:-2}"
      NCCL_CROSS_NIC: "${NCCL_CROSS_NIC:-1}"
      NCCL_CUMEM_ENABLE: "${NCCL_CUMEM_ENABLE:-0}"
      NCCL_IGNORE_CPU_AFFINITY: "${NCCL_IGNORE_CPU_AFFINITY:-1}"
      NCCL_DEBUG: "${NCCL_DEBUG:-WARN}"
      TORCH_CUDA_ARCH_LIST: "${TORCH_CUDA_ARCH_LIST:-12.1a}"
      FLASHINFER_CUDA_ARCH_LIST: "${FLASHINFER_CUDA_ARCH_LIST:-12.1a}"
      CUTE_DSL_ARCH: "${CUTE_DSL_ARCH:-sm_121a}"
      FLASHINFER_DISABLE_VERSION_CHECK: "${FLASHINFER_DISABLE_VERSION_CHECK:-1}"
      FLASHINFER_WORKSPACE_BASE: "${FLASHINFER_WORKSPACE_BASE:-/cache/huggingface/flashinfer}"

    command:
      - bash
      - -lc
      - >
        export PATH="/usr/local/cuda/bin:/usr/local/bin:$${PATH:-}";
        export CUDA_HOME="$${CUDA_HOME:-/usr/local/cuda}";
        export LD_LIBRARY_PATH="/usr/local/cuda/lib64:$${LD_LIBRARY_PATH:-}";
        exec vllm serve ${QWEN_VISION_MODEL:-RedHatAI/Qwen3.5-9B-quantized.w4a16}
        --host ${QWEN_VISION_HOST:-0.0.0.0}
        --port ${QWEN_VISION_PORT:-8890}
        --served-model-name ${QWEN_VISION_SERVED_MODEL:-qwen3.5-9b-vision}
        --tensor-parallel-size 2
        --nnodes 2
        --node-rank ${NODE_RANK}
        --master-addr ${MASTER_ADDR}
        --master-port ${MASTER_PORT:-25100}
        --distributed-executor-backend mp
        --max-model-len ${QWEN_VISION_MAX_MODEL_LEN:-32768}
        --max-num-seqs ${QWEN_VISION_MAX_NUM_SEQS:-5}
        --max-num-batched-tokens ${QWEN_VISION_MAX_NUM_BATCHED_TOKENS:-2048}
        --gpu-memory-utilization ${QWEN_VISION_GPU_MEMORY_UTILIZATION:-0.05}
        --kv-cache-memory-bytes ${QWEN_VISION_KV_CACHE_MEMORY_BYTES:-367001600}
        --kv-cache-dtype ${QWEN_VISION_KV_CACHE_DTYPE:-fp8}
        --enforce-eager
        --disable-custom-all-reduce
        --enable-chunked-prefill
        --enable-auto-tool-choice
        --tool-call-parser ${QWEN_VISION_TOOL_CALL_PARSER:-qwen3_coder}
        --reasoning-parser ${QWEN_VISION_REASONING_PARSER:-qwen3}
        --default-chat-template-kwargs '{"enable_thinking":false}'
        --trust-remote-code
        ${QWEN_VISION_HEADLESS:+--headless}
